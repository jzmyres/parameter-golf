[pytest]
# Single active test home. Archived rich-model tests live under legacy/tests/
# and are run explicitly only during the Phase-B contract port, not by default.
testpaths = tests
norecursedirs = legacy records baselines data .git __pycache__
filterwarnings =
    error::pytest.PytestReturnNotNoneWarning
