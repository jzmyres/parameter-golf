"""Archived legacy implementations.

``train_gpt_rich.py`` is the frozen rich RevDEQ/MoE trainer that was the
canonical ``train_gpt.py`` before the M0 promotion (see git tag
``legacy/rich-revdeq-moe``). It is kept importable so the archived contract
tests that froze its behaviour continue to validate it.
"""
